# Cascading Failure Simulator

A Python simulation framework for studying cascading failures, non-linear dynamics, feedback loops, and phase transitions in complex interconnected networks.

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Run initial system setup check
python main.py --topology barabasi_albert --nodes 100 --alpha 0.2

# Run test suite
pytest
```

